## Packages
framer-motion | Complex animations for wormhole effects and transitions
recharts | Visualizing burn data and quiz stats
lucide-react | Icons for the UI (already in base, but emphasizing use)
canvas-confetti | For the celebration effect on perfect quiz score
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
